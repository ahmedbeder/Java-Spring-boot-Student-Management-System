# Spring properties
spring:
  application:
    name: student-management-system
  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp


# HTTP Server
server:
  port: 8080 # HTTP (Tomcat) port
